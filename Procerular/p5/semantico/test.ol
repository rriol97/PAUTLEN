main {
    boolean z, y;

    function int doble(boolean arg; boolean b2) {
        boolean x, l;
        l = b2; 
        x = arg && l;
        return x;
    }

    scanf z;
    scanf y;
    printf doble(z, y);
}