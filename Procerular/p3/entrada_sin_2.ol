// Programa que eleva un numero al cuadrado

main {
  int x, resultado;

  class A {
		hidden unique boolean aa;
		secret boolean aa2;
		unique int aa3;
		boolean aa4;
		{A} aaa;
		function int mA1 (int p1) {
			return 2*p1;
		}
  };
  {A} a;

  scanf x;
  resultado = x*A.a(2);
  printf resultado;
}
